<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/quick_actions_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/madmax_surface_dark"
    android:paddingBottom="16dp">

    <!-- Drag Handle -->
    <com.google.android.material.bottomsheet.BottomSheetDragHandleView
        android:id="@+id/drag_handle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tint="#4A4A52" />

    <!-- Executive Header Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="20dp"
        android:paddingTop="2dp"
        android:paddingBottom="12dp">

        <!-- Header Icon Container (Rounded Badge) -->
        <FrameLayout
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/bg_icon_badge_primary">

            <ImageView
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_quick_actions"
                app:tint="@color/madmax_primary"
                android:contentDescription="Quick Actions" />
        </FrameLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingStart="14dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Terminal Actions"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#F4F4F6" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Quick controls, accessibility, snippets &amp; tools"
                android:textSize="11sp"
                android:textColor="#8E8E93" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/btn_close_quick_actions"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            app:tint="#8E8E93"
            android:contentDescription="Close" />

    </LinearLayout>

    <!-- Navigation Tabs -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/quick_actions_tab_layout"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:background="@color/madmax_surface_dark"
        app:tabMode="scrollable"
        app:tabGravity="start"
        app:tabIndicatorHeight="3dp"
        app:tabIndicatorColor="@color/madmax_primary"
        app:tabIndicatorFullWidth="false"
        app:tabSelectedTextColor="@color/madmax_primary"
        app:tabTextColor="#8E8E93"
        app:tabIconTint="@color/madmax_primary"
        app:tabInlineLabel="true"
        app:tabTextAppearance="@style/MadMax.TabTextAppearance" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#222228" />

    <!-- Content Container wrapped in NestedScrollView -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fillViewport="true"
        android:overScrollMode="never">

        <FrameLayout
            android:id="@+id/quick_actions_tab_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="240dp"
            android:paddingHorizontal="16dp"
            android:paddingTop="14dp"
            android:paddingBottom="16dp" />

    </androidx.core.widget.NestedScrollView>

</LinearLayout>
