<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/palette_item_card"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="3dp"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="#262630"
    app:cardBackgroundColor="#18181E"
    app:rippleColor="#25FFFFFF"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="10dp"
        android:paddingVertical="9dp">

        <!-- Icon Container -->
        <FrameLayout
            android:id="@+id/palette_icon_badge"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/bg_session_icon_container">

            <ImageView
                android:id="@+id/palette_item_icon"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_terminal"
                app:tint="#D0D0D4"
                android:contentDescription="Action Icon" />
        </FrameLayout>

        <!-- Title and Subtitle -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingStart="10dp"
            android:paddingEnd="8dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/palette_item_title"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textSize="13sp"
                    android:textStyle="bold"
                    android:textColor="#F2F2F5"
                    android:maxLines="1"
                    android:ellipsize="end"
                    tools:text="Clear Terminal Screen" />

                <!-- Category Tag -->
                <TextView
                    android:id="@+id/palette_item_category"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="9sp"
                    android:textStyle="bold"
                    android:paddingHorizontal="6dp"
                    android:paddingVertical="2dp"
                    android:textColor="#80DEEA"
                    tools:text="ACTION" />
            </LinearLayout>

            <TextView
                android:id="@+id/palette_item_subtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="#8E8E93"
                android:fontFamily="monospace"
                android:maxLines="1"
                android:ellipsize="end"
                android:paddingTop="2dp"
                tools:text="clear" />

        </LinearLayout>

        <!-- Execute Action Indicator -->
        <ImageView
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@drawable/ic_play"
            app:tint="#606068"
            android:contentDescription="Execute" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
